# ---------------------------------------------------------------------------
# Torch for a NATIVE macOS run (Apple Silicon).
#
# The default PyPI wheels for macOS-arm64 are built WITH the Metal (MPS)
# backend - unlike the linux/cpu wheels used by the Docker image. Do not point
# pip at download.pytorch.org/whl/cpu here or you lose GPU acceleration.
#
# Same versions as the container so behaviour stays comparable:
#   torch 2.11 + torchcodec 0.11 is what pyannote.audio 4.x needs.
# ---------------------------------------------------------------------------
torch==2.11.0
torchaudio==2.11.0
torchcodec==0.11.0
