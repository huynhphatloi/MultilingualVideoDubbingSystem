# torch 2.11 + torchcodec 0.11 is the first combination that ships linux
# *aarch64* wheels for torchcodec, which pyannote.audio 4.x requires.
# Without it the image cannot be built on Apple Silicon (linux/arm64).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0
torchaudio==2.11.0
torchcodec==0.11.0
