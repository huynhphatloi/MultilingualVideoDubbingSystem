[pytest]
testpaths = tests
# The suite must run with nothing installed beyond FastAPI and pytest: the
# provider split exists so that the registry, the API and the pipeline can be
# tested without torch, transformers or a GPU.
addopts = -q
